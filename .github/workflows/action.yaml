name: Hello-World

on:
  push:
    branches:
      - main

jobs:
    hello-world-job:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout code
            uses: actions/checkout@v4
          - name: Run a one-line script
            run: echo "$(cat hello_world.txt)"